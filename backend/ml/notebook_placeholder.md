ML Training Notebook
--------------------

Add a Jupyter notebook here (`training.ipynb`) that:
- Loads movie data (TMDB/MovieLens)
- Builds content-based features (genres, tags)
- Trains similarity model and saves `model.pkl`


